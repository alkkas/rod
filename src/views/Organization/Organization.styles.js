import styled from "styled-components";
import { Small } from "@components/GlobalStyles.styles";
const OrganizationTitle = styled(Small)`
  text-align: center;
`;

export { OrganizationTitle };
